/**
 * Abstract class supports polymorphism
 * "You can treat derived object as their abstract base type and call overridden method through virtual dispatch"
 */

/*
    the above statement states that abstract class enables polymorphism. Essentially we can interact with derived class objects as they were object of their common abstract class.

    the correct method for specific class is called at runtime.
*/

/*
    You can treat derived object as their abstract base type....
    it refers to upcasting.
    if we have a pointer or reference to to an abstract base class, 
    then we can point to an object of any class which is publicly deriving from abstract base class.
*/

/**
    virtual dispatch -> refers to runtime binding.
 */